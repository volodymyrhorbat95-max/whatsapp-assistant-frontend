# Backend API URL
VITE_API_BASE_URL=http://localhost:3000/api

# Admin API Key for backend authentication
# IMPORTANT: In production, use a secure method to provide this key
# Consider using a backend-for-frontend (BFF) pattern instead of storing
# the API key in the frontend code
VITE_ADMIN_API_KEY=your-secure-api-key-here
